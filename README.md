# Next.js ISR Minimal Sample

- Dev: `npm run dev`
- Build: `npm run build`
- Start: `npm run start`

## Pages
- `/` ホーム
- `/isr` ISR デモページ（`revalidate = 10` 秒）

## セットアップ
```bash
npm install
npm run dev
```
